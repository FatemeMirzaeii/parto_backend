const request = require('supertest');
const {  } = require("../../models");

describe('cycle', () => {

    beforeAll(async()=>{
       
    })
    beforeEach(async() => { 
        server=require('../../development');
    })
    afterEach(()=>{
         server.close();
    })
    afterAll(async () => {
 
    });

    describe('200', () => {
        it('200',async () => {
           
        });
    });
    
});